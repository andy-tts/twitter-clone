<%= form_with(model: hoot, local: true) do |form| %>
  <% if hoot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hoot.errors.count, "error") %> prohibited this hoot from being saved:</h2>

      <ul>
      <% hoot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :hoot_message %>
  </div>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
